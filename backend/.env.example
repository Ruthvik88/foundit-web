# Server
PORT=3000

# PostgreSQL connection string
DATABASE_URL=postgresql://user:pass@localhost:5432/campusclaim

# JWT secret — use a long random string in production
JWT_SECRET=change_me_to_a_long_random_string

# Cloudinary (optional — images work without it, stored as null)
CLOUDINARY_CLOUD_NAME=dpkhtqewp
CLOUDINARY_API_KEY=251977299523278
CLOUDINARY_API_SECRET=G8S4FIhC7Dkjo8hL0BTcVp0LLaM

# Email — Gmail SMTP (optional — claim notifications logged to console if not set)
EMAIL_USER=10c123ruthvikk@gmail.com
EMAIL_PASS=vzkqcxkqbokbdwdw

# SendGrid (optional — takes priority over Gmail SMTP if set)
# SENDGRID_API_KEY=

# Frontend URL for CORS
CLIENT_URL=http://localhost:5500
